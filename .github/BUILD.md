# Build APK

Acesse **Actions → Build Android APK → Run workflow** para compilar sob demanda.

A esteira também roda em push para `main` e em pull requests. O artifact contém:

- `app-debug.apk`
- `app-debug.apk.sha256`

O artifact é retido por 14 dias.
